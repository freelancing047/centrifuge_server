<?xml version="1.0" encoding="UTF-8"?>
<!-- When updating your version of GWT, you should also update this DTD reference, so that your app can take advantage of 
    the latest GWT module capabilities. -->
<!DOCTYPE module PUBLIC "-//Google Inc.//DTD Google Web Toolkit 2.5.0//EN"
  "http://google-web-toolkit.googlecode.com/svn/tags/2.5.0/distro-source/core/src/gwt-module.dtd">
<module rename-to="csi">
    <inherits name="csi.client.CSI" />
    <set-configuration-property name="CssResource.conversionMode" value="strict" />
    <!-- Dev only builds english, comment this out for i18n support -->
    <set-property name='locale' value="en_US"/>
 
    <!-- enable the SuperDevMode book marklets -->
    <add-linker name="xsiframe" />
    <set-property name="gwt.logging.developmentModeHandler" value="ENABLED" />

    
    <set-configuration-property name="devModeRedirectEnabled" value="true" />

    <!-- enable source maps -->
    <set-property name="compiler.useSourceMaps" value="false" />
	
    <set-configuration-property name="CssResource.style" value="pretty" />
    <!-- reduce permutations -->


    <!-- Dev only builds windows & chrome javascript, remove these lines to enable all -->
    <set-property name="user.agent" value="safari" />
    <set-property name="gxt.user.agent" value="chrome" />
    <set-property name="user.agent.os" value="windows" />
    <set-property name="gxt.device" value="desktop" />

    <collapse-all-properties value="false"/>
</module>
