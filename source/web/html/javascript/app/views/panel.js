/**
 * I define a certain area of the screen and display visualizations
 * for the user. I have a bar at the bottom of my area which describes
 * my contents and allows the user to switch between visualizations.
 */
define(["jquery", "backbone"], function($, Backbone) {
    return Backbone.View.extend({
	tagName: "div",
	
    });
});
